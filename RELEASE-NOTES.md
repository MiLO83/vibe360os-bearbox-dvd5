# BearBox DVD5 Kit Release Notes

## v0.1.0

Initial private release.

Includes:

- Disc 1 install/refresh ISO build
- Disc 2 live runtime / boot-key ISO build
- no fixed Linux username/password embedded by default
- interactive identity and storage in the normal install path
- explicit full-wipe install path
- dry-run-first non-installer disk wipe utility
- Dr. Watson decoy terminal
- authenticated browser terminal service
- Cloudflare Tunnel helper
- WebXR vibe runtime seed
- multiboot GRUB/os-prober setup

Do not publish generated `out/secrets/` contents.
